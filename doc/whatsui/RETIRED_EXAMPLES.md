# Retired Examples

## Todo App

状态：Retired（2026-08-01）

Todo App 已从 `examples/`、CMake target 和活动测试矩阵删除，不再构建、发布或维护。
Focus Tomato 取代它作为真实桌面产品与跨页面交互参考；Component Gallery 继续承担控件
覆盖与视觉检查职责。

文件名含 Todo 的旧验收记录、截图和故障复盘是历史证据，不代表当前可运行目标，也不会
作为未来发布门禁更新。新功能与回归测试不得再依赖 Todo 源码或可执行文件。

## Settings Panel 与 Command Palette

状态：Retired（2026-08-02）

两个独立示例已从 `examples/`、CMake target、CI 和活动测试矩阵删除，不再构建、发布
或维护。它们的专属参考文档仅作为历史设计记录保留。设置类控件、搜索/命令面板和弹窗
场景由 Component Gallery 覆盖；真实产品流程继续由 Focus Tomato 覆盖。
